<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Fund My Project!</title>
  <link rel="stylesheet" href="/css/style.css">
  <script src="https://cdn.tailwindcss.com"></script>


</head>

<body class="bg-rose-200">
      <nav class="bg-white dark:bg-lime-600 fixed w-full z-20 top-0 left-0 border-b border-gray-200 dark:border-gray-600">
        <div class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4">
          <span class="text-rose-200 underline font-mono text-[32px]">Tech-Blog</span>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/dashboard" 
          class="block py-2 pl-3 pr-4 text-gray-900 rounded hover:bg-gray-100 md:hover:bg-transparent md:border-0 md:hover:text-blue-700 md:p-0 dark:text-white md:dark:hover:text-blue-500 dark:hover:bg-gray-700 dark:hover:text-white md:dark:hover:bg-transparent">
          Dashboard</a> |
        <button id='logout'
          class='bg-blue-500 hover:bg-violet-700 text-white font-bold py-2 px-4 rounded absolute top-4 right-4'>Logout</button>
        {{else}}
        <a href="/login" 
          class="bg-white text-violet-600 font-bold py-3 px-6 rounded-lg border border-blue-600 hover:bg-blue-600 hover:text-white focus:outline-none focus:shadow-outline-blue">
          login</a>
        {{/if}}
        </div>
      </nav>
    <main class="">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
