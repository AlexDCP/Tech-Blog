<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech-Blog</title>
  <link rel="stylesheet" href="/css/style.css">
  <script src="https://cdn.tailwindcss.com"></script>


</head>

<body class="bg-rose-200">
      <nav class="bg-white dark:bg-lime-600 fixed w-full z-20 top-0 left-0 border-b border-gray-200 dark:border-gray-600">
        <div class="items-center justify-between hidden w-full md:flex md:w-auto">
          <span class="mr-4 text-rose-200 underline font-mono text-[32px]">Tech-Blog</span>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <div class="flex flex-row px-4 md:p-4 mt-4 space-x-4 justify-between">
        <a href="/homepage" 
          class=" py-2 pl-3 pr-4 bg-rose-200 hover:bg-violet-700 text-white font-bold rounded">
          Home</a>
        <a href="/dashboard" 
          class=" py-2 pl-3 pr-4 bg-rose-200 hover:bg-violet-700 text-white font-bold rounded">
          Dashboard</a>
        <button id='logout'
          class=' py-2 pl-3 pr-4 mr-4 bg-rose-200 hover:bg-violet-700 text-white font-bold rounded'>
          Logout</button>
        {{else}}
        <a href="/login" 
          class=" py-2 pl-3 pr-4 mr-4 bg-rose-200 hover:bg-violet-700 text-white font-bold rounded">
          login</a>
        </div>
        {{/if}}
        </div>
      </nav>
    <main class="">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
